<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thanh Toán Thành Công</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f3f4f6;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            text-align: center;
            background: #fff;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 400px;
        }

        .icon {
            font-size: 80px;
            color: #4caf50;
            margin-bottom: 20px;
        }

        .title {
            font-size: 24px;
            font-weight: 700;
            color: #333;
            margin-bottom: 10px;
        }

        .message {
            font-size: 16px;
            color: #555;
            margin-bottom: 20px;
        }

        /* .btn {
            display: inline-block;
            padding: 12px 20px;
            font-size: 16px;
            font-weight: 700;
            color: #fff;
            background: #4caf50;
            border: none;
            border-radius: 6px;
            text-decoration: none;
            transition: background 0.3s ease;
            cursor: pointer;
        }
        .btn:hover {
            background: #45a049;
        } */
    </style>
</head>

<body>
    <div class="container">
        <div class="icon">✔️</div>
        <div class="title">Thanh Toán Thành Công!</div>
        <div class="message">Cảm ơn bạn đã đặt lịch! Hệ thống của chúng tôi đã nhận được đơn hàng của bạn.</div>
        <!-- <a href="/mycart" class="btn">Quay về Giỏ Hàng</a> -->
    </div>
</body>

</html>